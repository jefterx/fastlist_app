import 'package:flutter/material.dart';
import 'app.dart';

void main() => runApp(const FastListApp());

class FastListApp extends StatelessWidget {
  const FastListApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const App();
  }
}
